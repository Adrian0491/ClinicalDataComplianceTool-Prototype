# requirements.txt
# =================================================
# Core dependencies for the ClinicalDataComplianceTool-Prototype
# This file contains the minimum packages needed to run the current MVP.
# It will be updated as the project grows (e.g., web UI, cloud integrations,
# reporting, testing, etc.).
# Last updated: January 10, 2026
# =================================================

# Data processing & SQL-based validation (replaces Polars/pandas)
duckdb>=1.0.0

# Anomaly / outlier detection (full replacement for scikit-learn)
pyod>=2.0.0

# Minimal numeric support (still needed for PyOD / basic math)
numpy>=1.26.0

# Optional / future additions (commented until needed)
# great-expectations>=0.18.0   # Declarative data validation & profiling
# pandera>=0.20.0              # Schema validation (no Polars needed)
# elasticsearch>=8.15.0        # Future real-time backend
# tensorflow>=2.15.0           # Optional deep learning anomalies
# loguru>=0.7.0                # Clean logging

# Install: pip install -r requirements.txt